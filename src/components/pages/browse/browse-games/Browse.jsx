'use client'

import { StyledBrowse } from "./Browse.styled";

export const Browse = ({ baseUrl, apiKey }) => {
    return (
        <StyledBrowse>
            <h1>BROWSE</h1>
        </StyledBrowse>
    )
}